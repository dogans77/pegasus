type Health={trained:boolean;deployed_model?:string;candidate_top1_accuracy?:number|null;candidate_top3_accuracy?:number|null;handicap_benchmark_top1_accuracy?:number|null;decision?:string;settled_races?:number|null;training_rows?:number|null};
const api=process.env.PEGASUS_API_URL ?? 'http://127.0.0.1:8042/api/v1';
const t={back:'← Programa dön',tag:'MODEL ŞEFFAFLIĞI',title:'Tahminin arkasındaki veriyi gör.',intro:'Pegasus, tahminleri kesin sonuç olarak değil; olasılık ve risk sinyali olarak sunar.',status:'MODEL DURUMU',ready:'Hazır',waiting:'Eğitim bekleniyor',top1:'Birinci aday isabeti',top3:'İlk üç kapsama',benchmark:'Handikap referansı',data:'Eğitim verisi',settled:'Sonuçlanmış koşu',how:'NASIL OKUNUR?',daily:'Her sabah program, gece ise sonuçlar yenilenir.',weekly:'Model, her pazar yerleşmiş sonuçlarla yeniden eğitilir.',note:'Bu ekran model izleme içindir; kesin sonuç iddiası taşımaz.'};
async function getJson<T>(path:string,fallback:T):Promise<T>{try{const r=await fetch(`${api}${path}`,{cache:'no-store'});return r.ok?await r.json() as T:fallback}catch{return fallback}}
function pct(value:number|null|undefined){return value==null?'--':`%${Math.round(value*100)}`}
export const dynamic='force-dynamic';
export default async function ModelPage(){const h=await getJson<Health>('/decision/model-health',{trained:false});return <main className="model-shell"><header className="model-nav"><a className="brand" href="/"><span>PEGASUS</span><small>RACE INTELLIGENCE</small></a><span className="model-links"><a href="/model-research">Araştırma</a><a href="/">{t.back}</a></span></header><section className="model-intro"><p className="eyebrow">{t.tag}</p><h1>{t.title}</h1><p>{t.intro}</p></section><section className="model-status"><article><p>{t.status}</p><h2>{h.trained?t.ready:t.waiting}</h2><span>{h.deployed_model??'--'}</span></article><article><p>{t.top1}</p><h2>{pct(h.candidate_top1_accuracy)}</h2><span>{h.decision??'--'}</span></article><article><p>{t.top3}</p><h2>{pct(h.candidate_top3_accuracy)}</h2><span>{t.benchmark}: {pct(h.handicap_benchmark_top1_accuracy)}</span></article><article><p>{t.data}</p><h2>{h.training_rows??h.settled_races??'--'}</h2><span>{t.settled}</span></article></section><section className="model-grid"><article className="model-panel"><p className="eyebrow">{t.how}</p><div className="method-list"><div><b>Olasılık</b><span>Her aday aynı koşudaki diğer atlarla birlikte değerlendirilir.</span></div><div><b>Chaos</b><span>Belirsizlik yüksek olduğunda tek aday yerine kapsama yaklaşımı kullanılır.</span></div><div><b>Value</b><span>Model olasılığı ile AGF piyasa sinyalinin farkı gösterilir.</span></div></div></article><article className="model-panel schedule"><p className="eyebrow">GÜNCELLEME DÜZENİ</p><b>{t.daily}</b><span>{t.weekly}</span><small>{t.note}</small></article></section></main>}
